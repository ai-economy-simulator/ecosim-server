export const valdiateToken = async (token: string) => {
  // Check out https://auth0.com/docs/secure/tokens/access-tokens/validate-access-tokens
  // Maybe just use Auth0 /userinfo endpoint to make sure token is valid?
  // Mock function for now
  return true;
};
